{
  plugins.noice = {
    enable = true;

    notify = {
      enabled = false;
    };

    messages = {
      enabled = true;
    };

    lsp.message.enabled = true;

    markdown = {
      highlights = true;
      hover = true;
    };

    popupmenu = {
      enabled = true;
      backend = "nui";
    };
  };
}
